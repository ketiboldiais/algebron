import MdxLayout from "@/components/mdx-layout";

# Complex Analysis

<TOC>

- [Complex Numbers](#complex-numbers)

</TOC>

## Complex Numbers
> __*definition*__. A _complex number_ is any number of the form ${a = z + ib,}$ where ${a}$ and ${b}$ are real numbers and ${i}$ is the imaginary unit.


export default function MDXPage({ children }) {
    return <MdxLayout>{children}</MdxLayout>
}